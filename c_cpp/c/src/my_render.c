#include "my_render.h"

void render_points(float                *positions, 
                   unsigned char        *rgb,
                   unsigned long        size)
{
    // your render code, probably using openGL.
}
